# 기본 포트
port 6379

# 보안 및 네트워크
bind 0.0.0.0
protected-mode no

# 지속성: AOF 활성화
appendonly yes
appendfsync everysec

# 메모리 정책 (옵션)
maxmemory 256mb
maxmemory-policy allkeys-lru

# 로그 레벨
loglevel notice

# Stream, ConsumerGroup 테스트를 위한 timeout 증가 (옵션)
timeout 300
